# Python dependencies for customer support LLM fine-tuning

# Core dependencies
pydantic>=2.0.0

# ML/AI framework dependencies
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
datasets>=2.12.0
peft>=0.5.0

# Optional but recommended
trl>=0.7.0  # Useful for SFT training
# bitsandbytes>=0.41.0   # For quantized inference/training (optional)
# sentencepiece>=0.1.99  # Some tokenizers require this

